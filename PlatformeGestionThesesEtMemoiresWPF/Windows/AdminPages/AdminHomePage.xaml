<Page x:Class="PlatformeGestionThesesEtMemoiresWPF.Windows.AdminPages.AdminHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PlatformeGestionThesesEtMemoiresWPF.Windows.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
            Title="AdminHomePage">




    <Grid Background="#FFDCC9A4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="175"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" BorderBrush="#5c5c5c3b" BorderThickness="1" Background="#FFF1E8D6" Margin="10,10,10,10">
            <StackPanel Margin="10">
                <!-- Logo/App Name -->
                <TextBlock Text="My App" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center" Foreground="#FF314E3D"/>

                <!-- Navigation Items -->
                <StackPanel>
                    <!-- Home Button -->
                    <Border CornerRadius="4" Height="40" Background="#FF314E3D" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="Home" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                Foreground="White" 
                                FontSize="14" 
                                FontWeight="SemiBold"
                              >
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter HorizontalAlignment="Left" 
                                                                  VerticalAlignment="Center"
                                                                  Margin="10,0,0,0"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#4290f7"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#3983e4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                    <!-- Acounts Button -->
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="Searshe these" 
Background="Transparent" 
BorderThickness="0" 
Foreground="#FF314E3D" 
FontSize="14" 
FontWeight="SemiBold" 
Width="153" Click="Button_Click_5">

                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Padding" Value="10,0,0,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Left" 
                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#4290f7"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="#E8F0FE" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="Transparent" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="Accounts" 
        Background="Transparent" 
        BorderThickness="0" 
        Foreground="#FF314E3D" 
        FontSize="14" 
        FontWeight="SemiBold" 
        Click="Button_Click" 
        Width="153">

                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Padding" Value="10,0,0,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#4290f7"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#E8F0FE" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="Transparent" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <!-- Profile Button -->
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="theses" 
        Background="Transparent" 
        BorderThickness="0" 
        Foreground="#FF314E3D" 
        FontSize="14" 
        FontWeight="SemiBold"
        HorizontalAlignment="Stretch"
        Click="Button_Click_1">

                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Padding" Value="10,0,0,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#4290f7"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#E8F0FE" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="Transparent" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                    <!-- new theses Button -->
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="new these" 
        Background="Transparent" 
        BorderThickness="0" 
        Foreground="#FF314E3D" 
        FontSize="14" 
        FontWeight="SemiBold"
        HorizontalAlignment="Stretch" 
        Click="Button_Click_2">

                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Padding" Value="10,0,0,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#4290f7"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                 To="#E8F0FE" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                 To="Transparent" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                    <!-- Profile Button  
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="Profile" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                Foreground="#333" 
                                FontSize="14" 
                                FontWeight="SemiBold"
                                HorizontalAlignment="Stretch"
                             >
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter HorizontalAlignment="Left" 
                                                                  VerticalAlignment="Center"
                                                                  Margin="10,0,0,0"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E0E0E0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    -->


                    <!-- Settings Button -->

                    <!-- Separator -->
                    <Border BorderBrush="#5c5c5c3b" BorderThickness="0,1,0,0" Margin="0,0,0,20"/>

                    <!-- Logout Button 
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand">
                        <Button Content="Logout" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                Foreground="#333" 
                                FontSize="14" 
                                FontWeight="SemiBold"
                                HorizontalAlignment="Stretch"
                              >
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter HorizontalAlignment="Left" 
                                                                  VerticalAlignment="Center"
                                                                  Margin="10,0,0,0"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E0E0E0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border> -->

                    <!-- Logout Button -->
                    <Border CornerRadius="4" Height="40" Background="Transparent" Cursor="Hand" Margin="0,0,0,10">
                        <Button Content="Logout" 
        Background="Transparent" 
        BorderThickness="0" 
        Foreground="#FF314E3D" 
        FontSize="14" 
        FontWeight="SemiBold"
        HorizontalAlignment="Stretch" Click="Button_Click_3">

                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{x:Null}">
                                    <Setter Property="OverridesDefaultStyle" Value="True"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Padding" Value="10,0,0,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#4290f7"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#E8F0FE" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="Transparent" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>


                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Rest of your existing content remains unchanged -->
        <!-- Content Area -->
        <Grid Grid.Column="1" Margin="10 ">
            <!-- ... [Your existing content grid remains exactly the same] ... -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- matricule Row -->
            <Border CornerRadius="4" Grid.Row="0" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="0,5" Height="32">
                <TextBox x:Name="tbsearch_Matricule" Text="Matricule" Padding="10 5" Background="#FFF1E8D6" BorderThickness="0"
                       GotFocus="matricule_GotFocus" LostFocus="matricule_LostFocus" FontWeight="Bold" FontFamily="Times New Roman" FontSize="14" Foreground="#FF314E3D" />
            </Border>

            <!-- first name/last name Row -->
            <Grid Grid.Row="1" Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border CornerRadius="4" Grid.Column="0" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="0,0,5,0" Height="32">
                    <TextBox x:Name="tbsearch_FName" Text="First Name" Padding="10 5" Background="#FFF1E8D6" BorderThickness="0"
                          GotFocus="first_GotFocus" LostFocus="first_LostFocus" FontWeight="Bold" FontFamily="Times New Roman" FontSize="14" Foreground="#FF314E3D"/>
                </Border>
                <Border CornerRadius="4" Grid.Column="1" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="5,0,0,0">
                    <TextBox x:Name="tbsearch_LName" Text="Last Name" Padding="10 5" Background="#FFF1E8D6" BorderThickness="0"
                          GotFocus="last_GotFocus" LostFocus="last_LostFocus" FontWeight="Bold" FontFamily="Times New Roman" FontSize="14" Foreground="#FF314E3D"/>
                </Border>
            </Grid>

            <!-- username Row -->
            <Grid Grid.Row="2" Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border CornerRadius="4" Grid.Column="0" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="0,0,5,0" Height="32">
                    <TextBox x:Name="tbsearch_Username" Text="Username" Padding="10 5" Background="#FFF1E8D6" BorderThickness="0"
                          GotFocus="username_gotFocus" LostFocus="username_LostFocus" FontWeight="Bold" FontFamily="Times New Roman" FontSize="14" Foreground="#FF314E3D"/>
                </Border>
                <!-- <Border CornerRadius="4" Grid.Column="1" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="5,0,5,0">
                        <TextBox Text="email" Padding="10 5" Background="Transparent" BorderThickness="0"
                         TextChanged="TextBox_TextChanged" GotFocus="university_GotFocus" LostFocus="university_LostFocus"/>
                    </Border>
                    <Border CornerRadius="4" Grid.Column="2" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="5,0,0,0">
                        <TextBox Text="NIN" Padding="10 5" Background="Transparent" BorderThickness="0"
                         TextChanged="TextBox_TextChanged" GotFocus="faculte_GotFocus" LostFocus="faculte_LostFocus"/>
                    </Border>-->

                <Border CornerRadius="4" Grid.Column="1" Height="32" Background="#FF314E3D" Margin="5,0,0,0" Cursor="Hand">
                    <Button Content="reset"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Background="Transparent"
        BorderThickness="0"
        Click="reset"
        Foreground="White"
        FontSize="18"
        FontWeight="Bold"
        FontFamily="Times New Roman">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="OverridesDefaultStyle" Value="True"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#4290f7"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
To="#1E232A" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
To="#FF314E3D" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
                <Border CornerRadius="4" Grid.Column="2" Height="32" Background="#FF314E3D" Margin="5,0,0,0" Cursor="Hand">
                    <Button Content="search"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Background="Transparent"
        BorderThickness="0"
        Click="Button_Click_4"
        Foreground="White"
        FontWeight="Bold"
        FontFamily="Times New Roman"
        FontSize="18">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="OverridesDefaultStyle" Value="True"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="White"/>
                                <!-- Keep it consistent -->
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#4290f7"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
To="#1E232A" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
To="#FF314E3D" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
            </Grid>



            <!-- Results Area -->
            <Border Grid.Row="3" BorderBrush="#5c5c5c3b" BorderThickness="1" Margin="0,10">
                <!-- <Image Source=".\images\icons8-books-48 (1).png" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->
            </Border>
        </Grid>
        <ScrollViewer Grid.Column="1" Margin="10,147,10,20">
            <StackPanel x:Name="stack_panel_user" Background="#FFF1E8D6">
                <!-- result are-->
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>
